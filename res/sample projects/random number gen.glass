import System;
import Generic;
import Collections.DataTypes;

int random = Random.range(0,100);
float uniform = Random.Uniform(0.75, 10);

Shout("Random number: ");
Shoutln(random);

Shout("Uniform number: ");
Shoutln(uniform);

GetKey("");
